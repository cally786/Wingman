import 'expo-router/entry';
import { View } from 'react-native';

// Este componente nunca se renderiza, pero satisface el requisito de exportación por defecto
export default function App() {
  return <View />;
} 